[{kernel,
  [
    {distributed, [
      {storage, 5000, [storage@localhost, world@localhost]}
    ]},

    {sync_nodes_mandatory, [world@localhost]},
    {sync_nodes_optional, [chat@localhost]},
    {sync_nodes_timeout, 5000}
  ]
}].
