[{kernel,
    [
        {distributed, [
            {chat, 5000, [chat@localhost, world@localhost]}
        ]},

        {sync_nodes_mandatory, [world@localhost]},
        {sync_nodes_optional, [storage@localhost]},
        {sync_nodes_timeout, 5000}
    ]
}].
